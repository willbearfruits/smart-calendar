# Optional: Backend API URL (defaults to http://localhost:3001)
# VITE_API_URL=http://localhost:3001

# Note: The Gemini API key is now stored in server/.env
# This keeps the API key secure and never exposed to the frontend
